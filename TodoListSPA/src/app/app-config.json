{
  "auth": {
    "clientId": "Enter the Client Id (aka 'Application ID')",
    "authority": "https://login.microsoftonline.com/common",
    "validateAuthority": true,
    "redirectUri": "Enter the Redirect Uri that you set on Azure Portal, like http://localhost:4200",
    "postLogoutRedirectUri": "Enter the Uri that you wish the app to redirect to after logout, like http://localhost:4200",
    "navigateToLoginRequestUrl": true
  },
    "cache": {
        "cacheLocation": "localStorage"
    },
    "scopes": {
        "loginRequest": ["openid", "profile"]
    },
    "resources": {
      "todoListApi": {
        "resourceUri": "Enter the TodoList Web APIs base address, e.g. 'https://localhost:44351/api/todolist'",
        "resourceScope": "Enter the API scopes as declared in the app registration 'Expose an Api' blade in the form of 'api://{clientId}/access_as_user'"
      }
    }     
}
